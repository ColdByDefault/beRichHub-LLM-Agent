VECTOR_STORE=astra # or 'postgres'
ASTRA_DB_NAMESPACE="default_keyspace" # Replace with your actual keyspace name or use "default_keyspace" if you haven't set one up.
ASTRA_DB_COLLECTION='' # Replace with your actual collection name or leave empty if not applicable.
ASTRA_DB_API_ENDPOINT="" # Replace with your actual Astra DB API endpoint or leave empty if not applicable.
ASTRA_DB_APPLICATION_TOKEN="" # Replace with your actual Astra DB application token or leave empty if not applicable.
OLLAMA_API_URL=http://localhost:11434 # Replace with your actual Ollama API URL or leave as is if running locally.
OLLAMA_CHAT_MODEL=<model_name>
OLLAMA_EMBED_MODEL=<model_name>
EMBEDDING_DIMENSION=<dimension>
NEXT_PUBLIC_OLLAMA_API_URL=http://localhost:11434 # Replace with your actual Ollama API URL for public access or leave as is if running locally.
POSTGRES_URL=postgresql://user:pass@localhost:5432/yourdb # Replace with your actual Postgres URL or leave as is if not using Postgres.
# If using Postgres, ensure the following environment variables are set:
POSTGRES_USER=user # Replace with your actual Postgres user or leave as is if not using Postgres.
POSTGRES_PASSWORD=pass # Replace with your actual Postgres password or leave as is if not using Postgres.
PDF_INGESTION_ENABLED=false # Set to true if you want to enable PDF ingestion, otherwise false. NOTE: This is not currently implemented.
